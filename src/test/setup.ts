// Setup para testes
import { afterAll, beforeAll } from 'vitest';

beforeAll(async () => {
  // Setup do banco de dados de teste se necessário
});

afterAll(async () => {
  // Cleanup do banco de dados de teste se necessário
});
